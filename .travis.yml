language: java
jdk:
  - openjdk8
branches:
  - master

# Travis  CI 서버의 Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'
script: "./gradlew clean build"

# CI 실행 완료시 메일로 알람
notifications:
  email:
    recipients:
      - gksqlcxkdns1@naver.com

# ./gradlew 파일의 권한문제 해결을 위한 방안
before_install:
  - chmod +x gradlew
